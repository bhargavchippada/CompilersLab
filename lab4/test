int remake(int z){
	return 3*4;
}

int swap(int p, int q){
	int x;
	x=1;
	return 0;
}

int mainn()
{
	int x, y;
	float p[4][5];
	p[4][3] = swap(1,1.2);
	y = 2&&1.5;
	//swap = 30;
	// int z[200];

	for (x=0.5; x <10; x++);
	if (y >1) {x=x-1; y=y+1;} else ;
}
